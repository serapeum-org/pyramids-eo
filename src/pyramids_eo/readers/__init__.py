"""Instrument readers — the package entry points.

Each reader takes a local file / path / bytes and returns a pyramids
``Dataset`` / ``DatasetCollection`` (or curvilinear equivalent). See the
package roadmap in ``planning/scope/eo-package-and-readers-plan.md``.
"""

from __future__ import annotations

__all__: list[str] = []
